module app.models {
    export interface IUser {
        
    }

    export class User implements IUser {
        private name: string;
        private lastName: string;
        private age: number;


    }
}